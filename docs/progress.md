# Arcana 项目进度

最后更新：2026-07-30

## 当前状态

- 当前阶段：P4 正式应用分享与扩展牌阵迁移
- 整体状态：进行中
- 当前目标：完成凯尔特十字微信真机验收，再迁移圣三角、恋爱关系五牌阵与二选一牌阵
- 当前阻塞：无代码阻塞；分享与抽牌交互仍需微信真机验收，个人主体审核风险已记录并暂缓处理

## 已完成

### 2026-07-20

- 建立项目章程。
- 明确 Web 先行、微信小程序后续迁移。
- 确认仓库和工程代号为 `arcana`。
- 确认 React + TypeScript + Taro 技术方向。
- 定义 MVP 范围和暂不纳入项。
- 建立跨端架构和平台适配边界。
- 确认标准 78 张塔罗体系。
- 建立牌组可扩展的数据设计。
- 完成阶段化开发计划和验收标准。
- 建立 `prototype/` 独立可点击原型工程。
- 确认牌阵优先级：单牌、时间流、圣三角、恋爱关系五牌阵、二选一牌阵。
- 将牌义、牌组素材映射和牌阵配置拆分为三套独立 JSON。
- 页面改为通过领域目录组合牌义与牌组素材，不再写死牌义、图片或牌阵位置。
- 历史记录增加内容版本、牌组版本和牌阵版本。
- 增加数据关联完整性自动测试。
- 本次结构化数据改造已创建独立 Git 提交。
- 完成首页、问题输入、洗牌、抽牌、翻牌、解读结果和历史记录。
- 完成完整22张大阿卡纳结构化牌义。
- 为原型牌组补齐22张大阿卡纳素材映射。
- 建立牌义、牌组和牌阵三类 JSON Schema。
- 增加 `npm run data:validate` 独立校验命令。
- 增加感情、职场、家庭、心情四类问题引导。
- 每类增加4个具体选项，选择后自动带出可编辑的开放式问题。
- 问题分类与选项改为独立 JSON，并纳入 Schema 与完整性校验。
- 历史记录保存问题分类 ID 和选项 ID。
- 确认首套正式牌面采用1909/1910年代原始 Rider–Waite–Smith 完整78张。
- 建立 RWS 原始扫描、Web 优化文件、牌背、来源证据和 SHA-256 管理规范。
- 为16个问题选项配置单牌或时间流推荐牌阵。
- 正式启用时间流牌阵。
- 完成过去、现在、未来三张无重复抽取、逐张翻牌和分牌位解读。
- 历史记录支持保存多张牌及对应牌位。
- 增加抽牌去重和固定随机源可复现测试。
- 确定正式牌义的分层 JSON 模型、运行时组合规则和结果页字数规范。
- 明确现有22张大阿卡纳短文案属于原型草稿，后续迁移但不直接作为正式牌义发布。
- 建立牌义 Schema v2 和问题分类到牌义主题的显式映射。
- 完成“愚人”分层牌义样例，包含象征、正逆位明暗面、建议、反思与六类主题。
- 增加 v1 → v2 编辑草稿迁移脚本，22张现有牌均可生成通过 Schema 的迁移草稿。
- 增加 Schema v2、主题映射和迁移完整性自动测试。
- 完成结构化解读组合器，统一基础牌义、正逆位、问题主题与牌阵位置。
- 单牌结果页改为只读取结构化展示模型，并分层展示摘要、详情、象征、明暗面、建议和反思问题。
- 多牌阵继续使用摘要式展示，避免三牌或五牌结果过长。
- 未完成 v2 编辑的21张牌通过 v1 适配层继续可用，结果页不再直接读取旧牌义字段。
- 建立标准78张卡牌索引：22张大阿卡纳，权杖、圣杯、宝剑、钱币各14张。
- 建立 RWS 正式牌组元数据、78张素材 Manifest 和独立牌背配置。
- 原图与 Web 图均支持记录路径、格式、尺寸、大小和 SHA-256。
- 增加素材状态流转、路径边界、文件元数据、哈希和授权门槛校验。
- 增加固定参数 WebP 处理命令，保持原始扫描文件不变。
- 78张正式素材最初以 `pending-source` 建槽，未写入未经核验的图片或授权结论。
- 完成 Wikimedia Commons TaionWC Pam-A 集合的78文件 API 审计。
- 固化78个文件页、原图地址、当前版本、SHA-1、尺寸和许可元数据。
- 确认78个文件均标记 Public domain、Public Domain Mark 和 `PD-old-80-expired`。
- 项目方接受 Commons 权利判断及免责声明，许可状态更新为 `approved`。
- 下载并保存78张未经修改的 Pam-A JPEG 原图，全部通过 Commons SHA-1、字节数、尺寸和格式交叉核对。
- 为78张原图计算并记录项目 SHA-256，使用固定参数生成 WebP 并记录完整元数据。
- 生成78张总览并检查完整性、顺序、重复、混版和异常裁切，全部素材状态更新为 `approved`。
- 建立完整 RWS Web 图片映射，Web 原型从符号占位牌切换为正式牌面。
- 加入正逆位、跳过动效、本地保存和减少动态效果支持。
- 生成 Arcana 专属社交分享预览图。
- 更新原型说明和服务端渲染测试。
- 验证原型生产构建和自动测试通过。
- 创建 P1 原型里程碑提交 `b30aa43`。
- 将 `.npmrc` 镜像配置纳入原型仓库，提交 `75de867`。
- 发布私有评审版本：`https://arcana-tarot-prototype.lostme037.chatgpt.site`。
- 将 Git 结构整理为 Arcana 根目录唯一主仓库，`prototype/` 改为普通目录。
- 原型旧仓库历史已在迁移前完成临时 bundle 备份。

### 2026-07-21

- 将首页、问题分类、问题确认、牌阵选择和解读改为单页引导式对话流。
- 新增纯 TypeScript 对话阶段 reducer；无效阶段动作会被守卫，用户选择固化为消息记录。
- 抽牌阶段从对话流中独立为全屏工作区；抽完后自动返回对话，并在消息中展示牌阵和完整解读。
- 问题选择改为渐进式披露：先选分类，再选或填写问题，最后选择牌阵。
- 牌阵选择改为左右卡片布局，并使用数据驱动的 SVG 牌阵示意图；同牌数不同布局可独立配置。
- 为牌阵视觉坐标增加 Schema、类型和“每个牌位对应一张示意牌”的校验。
- 抽牌改为用户直接点击牌背；牌组支持鼠标与触摸拖动。
- 时间流在抽牌页显示过去、现在、未来牌框，默认按牌位顺序抽取，也允许手动切换空牌位。
- 选牌动画从实际点击牌背的位置升起，缓慢翻面后精确缩放并落入目标牌框。
- 使用真实容器坐标和动画结束事件消除落位偏差；浏览器实测目标误差低于0.01像素。
- 进入抽牌页先播放3秒洗牌动画；减少动态效果模式缩短为150毫秒，并允许跳过。
- 牌组改为下沉到屏幕底部之外的圆环，只展示顶部扇面；首尾保留缺口，拖动时旋转整个牌环。
- 点击、轮盘拖动、洗牌完成和抽牌完成接入统一 Haptics 适配层；Web 使用可用时的 `navigator.vibrate`，为微信小程序预留替换入口。
- 拆出聊天线程、牌阵图标和牌面展示组件，保留抽牌、组合解读和历史记录领域逻辑。
- 将22张大阿卡纳全部迁移为独立分层 v2 草稿，补齐正逆位、象征、明暗面、建议、反思和六类主题解读。
- 单牌默认展示完整分层解读。
- 时间流等多牌阵改为消息流「照耀 / 建议」摘要，并提供独立详解页阅读完整分层内容；摘要不复述问题与牌位点名句式。
- v2 数据校验改为自动检查全部大阿卡纳文件、稳定 ID 顺序、六类主题和编辑占位文本。
- 新增对话状态机测试；数据校验、素材校验、SSR、ESLint 和生产构建全部通过。
- 已在浏览器走通单牌与时间流，对话自动滚动、独立抽牌页、自动返回、摘要与详解页、历史保存均通过人工验证。
- 深度润色 major-01 至 major-21：删除跨牌模板句与关键词插槽文案，重写正逆位与六类主题；优先修正恶魔、死神、倒吊人、高塔、月亮等语义冲突。
- 新增 `content-quality.mjs` 确定性质量门槛，并接入 `data:validate` 与回归测试。
- 引导文案调整为更神秘的占卜语气；抽牌页标题避免被圆环容器裁切。
- 完成22张大阿卡纳代理内容审查：收紧死神、高塔等高风险措辞，软化绝对/恐吓句，全部标记 `editorial.status: approved`（`contentVersion` 1.1.0）；不再把独立人工盲审作为过审门槛。

- 完成权杖花色 14 张小阿卡纳正式 v2：Schema 支持 minor，`validate-data` 校验权杖齐套与质量门槛；运行时抽牌池仍仅 22 张大阿卡纳。
- 完成圣杯花色 14 张小阿卡纳正式 v2（已 `approved`）；校验已覆盖权杖与圣杯齐套。
- 完成宝剑花色 14 张小阿卡纳正式 v2（已 `approved`）；校验已覆盖权杖 / 圣杯 / 宝剑。
- 完成钱币花色 14 张小阿卡纳正式 v2（已 `approved`）；四花色 56 张小阿卡纳齐全，质量门槛通过。
- 扩展运行时抽牌池到 78 张：同步 `registry.ts` 与 `card-meanings.json`，新增 `sync-runtime-deck.mjs`。
- 启用圣三角牌阵：牌阵可选、摘要按现状/挑战/建议组织；职场与迷茫类问题推荐圣三角。
- 启用恋爱关系五牌阵：摘要含我/对方/关系/挑战与趋势建议，并保留「对方非读心」边界说明；感情类问题推荐该牌阵。
- 启用二选一牌阵：抽牌前必填 A/B，摘要并列处境与两条路径过程/倾向，不宣布胜者；职场转行与迷茫纠结推荐该牌阵。
- 实现今日一牌：欢迎页入口、按本地日期固定抽牌与正逆位，同日再次进入直接回看。
- 接入 Arcana 原创牌背 `arcana-starpath-mirror`（PNG 源文件 + WebP 运行时）。
- 实现分享卡片：结果页生成夜间金饰风格图片，优先 Web Share，失败则复制文案且不影响查看与保存。

### 2026-07-22

- 修复五牌阵分享图的固定高度截断，使用完整五张牌的紧凑双列布局并增加回归测试。
- 增加安全本地存储适配层；隐私模式、权限限制或容量异常不再中断每日一牌、历史保存与结果流程。
- 将原创牌背源 PNG 移出公开目录，归档原图与 WebP 分离管理。
- 为原创牌背补齐作者、版本、创建日期、许可、尺寸、字节数和 SHA-256，并纳入素材自动校验与公开 WebP 同步。
- 补齐 Cloudflare Worker 环境类型声明，新增 `npm run typecheck` 并纳入完整测试门禁。
- 在 `app/` 初始化正式 Taro 4 + React + TypeScript 工程，与 `prototype/` 保持分离。
- 配置 H5 和微信小程序双构建目标，并加入 Zustand、Vitest、ESLint、Prettier 和统一验证命令。
- 建立纯 TypeScript 抽牌领域示例、可注入随机源与3项自动测试。
- 建立正式工程的存储、触觉适配层和会话状态骨架；业务页面不直接调用平台 API。
- 建立午夜靛蓝、旧金色和纸张暖白的跨端设计令牌与基础页面。
- 验证 H5 正式产物包含完整 JS/CSS 入口，微信小程序产物可生成 `app.json`、页面 WXML/WXSS/JS。
- 将78张牌义、牌阵、问题引导、牌组 Manifest、Schema、素材与纯领域规则提取到 `packages/tarot-core`。
- 原型通过兼容导出继续使用共享领域实现，数据与素材脚本改为读取唯一共享来源。
- 正式 Taro 应用接入共享牌库、抽牌规则和解读组合器，完成“选择分类—确认问题—抽取单牌—查看分层解读—重新开始”的最小闭环。
- H5 与微信小程序构建自动复制正式牌面和原创牌背，不在页面硬编码素材路径。
- 新增正式应用共享牌库单牌组合测试；原型41项测试与正式应用双端构建均通过。
- 正式应用单牌流程增加洗牌等待、七张牌背选牌和翻牌显现动画，触觉反馈仍通过平台适配层调用。
- 增加跨端本地历史记录：最多保存12条，支持保存失败提示、详情回放和删除。
- 历史数据清洗、数量限制、置顶保存与删除增加2项测试；正式应用当前共6项测试通过。
- 正式应用迁移今日一牌：按本地日期稳定抽取，同日再次进入直接回看，并复用单牌洗牌、翻牌、解读和历史保存能力。
- 新增正式应用今日一牌组合测试；类型、Lint、格式、7项测试以及 H5 / 微信小程序构建通过。
- 正式应用迁移时间流：支持在单牌与时间流之间选择牌阵，按过去、现在、未来依次选取并翻开三张不重复牌。
- 时间流结果增加照耀与建议摘要、三牌概览和可展开的逐张完整解读。
- 历史记录升级为通用多牌模型，兼容旧版单牌存储，并支持时间流保存、详情回放和删除。
- 新增时间流与多牌历史测试；正式应用类型、Lint、格式、10项测试以及 H5 / 微信小程序构建通过。

### 2026-07-23

- 复核正式应用最近三个功能提交：`696856e`（共享核心与单牌）、`9ae9c76`（今日一牌）、`3b242c6`（时间流）。
- 确认 P2 工程基础已完成：`packages/tarot-core` 成为牌义、牌阵、牌组素材和纯领域规则的唯一来源，原型与正式应用均复用该包。
- 确认 P3 单牌代码闭环已完成：问题分类、问题确认、洗牌、七张候选牌视觉入口、翻牌、分层解读、本地保存、历史回放和删除均已接入正式 Taro 应用。
- 确认今日一牌和时间流已进入正式应用；时间流复用通用多牌历史模型，并兼容此前保存的单牌记录。
- 项目阶段调整为 P4 扩展牌阵迁移；圣三角、恋爱关系五牌阵和二选一牌阵的领域配置与组合摘要已存在于共享核心，下一步接入正式应用界面。
- 确认微信小程序牌面采用现有 CloudBase 环境，不引入独立 CDN；Web 继续使用本地静态资源。
- 增加 Web/微信素材地址适配层和 CloudBase 初始化，微信路径统一由平台素材适配层生成 CloudBase File ID。
- 自动验证愚人、恋人、星星三张样例牌及全部78张牌的 CloudBase 文件 ID 映射。
- 调整 Taro 复制配置：H5 仍包含78张正面牌，微信构建只保留独立牌背。
- 微信构建产物由约18 MB 降至1,146,316字节，正面牌文件数量为0。

### 2026-07-24

- 为 `prototype` 增加 `predev`，本地启动前自动同步78张牌面和牌背到公开目录。
- 为 `app` 增加项目级 npm/pnpm 与 Corepack 淘宝镜像配置。
- 微信开发者工具项目切换到正式 AppID `wxdee65d504fb068d2`。
- CloudBase 环境更新为 `cloud1-d4gihrh6ob576fe1d`，牌面位于存储根目录。
- 增加 File ID 到临时 HTTPS 地址的解析，避免渲染层把 `cloud://` 当成本地相对路径。
- 验证云文件存在，但公开读取因 `STORAGE_EXCEED_AUTHORITY` 受阻；决定付费前使用本地素材模式。
- 增加 `dev:weapp:local` 和 `build:weapp:local`，本地构建复制全部78张牌面，产物约18 MB且仅用于开发者工具。
- 移除正式页面对Zustand store的依赖，规避Taro小程序产物将 `create` 错绑到React的运行时错误。
- 选牌区从静态绝对定位改为微信原生横向 `ScrollView`，支持触摸左右滑动。
- 明确当前7张候选牌背仍是仪式入口，未与78张洗牌结果逐张绑定；真实选牌体验列为下一项。
- 正式应用类型检查、15项测试及本地素材小程序构建通过。
- 根据备案口径完成公开文案整改：功能统一表述为卡牌解读、记录与反思，移除容易被理解为结果判断或心理治疗的表述。
- 导航栏和分享品牌统一为“阿卡纳星语”；时间流与关系牌阵统一使用“后续关注”。
- Web原型同步收敛旧版神秘化文案，避免H5和微信小程序定位不一致。
- 新增运行时高风险文案扫描测试，覆盖正式应用、共享牌义和Web原型。

### 2026-07-27

- 完成微信小程序个人主体文案与问题提示审查；确认当前风险主要来自完整塔罗交互形态，而非单个敏感词，项目决定暂不裁剪，提审前再复核。
- 将正式应用的7张视觉候选升级为完整78张位置选牌：进入洗牌阶段时生成唯一洗牌顺序和正逆位，点击牌背位置决定实际抽牌。
- 多牌抽取保留同一副准备牌组，已选择位置会被锁定，避免同次解读重复选择。
- 今日一牌不再显示无实际作用的多张候选牌，改为单张牌背直接翻开，同时保持同一自然日结果稳定。
- 共享领域层增加完整牌组准备和按位置选牌接口，页面不直接实现洗牌或位置校验规则。
- 增加完整牌组一一映射、点击位置决定结果、重复位置和越界位置拒绝测试。
- 正式应用类型、ESLint、格式、18项测试以及 H5 / 微信小程序生产构建通过。
- 浏览器自动接管受当前桌面运行环境限制，本轮未将人工视觉流程标记为通过；仍需在微信开发者工具和真机验证长距离横向滚动、重复位置锁定与多牌连续抽取。
- 修复今日一牌在小程序状态批处理下可能被普通问题草稿守卫静默拦截的问题；今日模式改为使用固定分类，不再依赖异步写入的问题草稿。
- 翻牌素材解析失败时显示可重试提示，不再表现为点击无响应。
- 重新生成本地素材微信构建；18项测试、类型检查、ESLint和微信生产构建通过。
- 确认微信开发者工具私有配置重新开启热重载，导致继续运行旧 CloudBase bundle；本地关闭热重载并要求清缓存后重编译。
- 将 `dev:weapp` 和 `build:weapp` 默认调整为本地素材模式；CloudBase模式改用显式 `:cloud` 命令，完整验证结束后会恢复本地 `dist`，避免开发构建再次被云模式覆盖。
- 真机调试发现牌背未显示；确认牌背文件已进入包内，但开发者工具开启“忽略未使用文件”后可能将 JSON 动态路径引用的牌背误判并排除。项目配置与本地私有配置均关闭该优化，并增加配置回归测试。

## 正在进行

- 正式应用单牌、今日一牌和时间流的微信开发者工具评审。
- 正式应用 Web 与微信分享真机评审。
- CloudBase牌面按需加载的持续真机验收。
- 微信审核前的全量截图、分享文案和隐私说明复核。

## 下一步

1. 在微信开发者工具和真机验证凯尔特十字十牌位置、交叉位、连续抽取与结果滚动。
2. 在 H5 验证 PNG 分享卡、系统分享和剪贴板降级，在微信真机验证好友与朋友圈入口。
3. 迁移圣三角牌阵，复用现有多牌抽取、组合摘要和通用历史模型。
4. 迁移恋爱关系五牌阵与二选一牌阵，并处理二选一的 A/B 输入。

## 问题与风险

- Rider–Waite–Smith 正面牌已完成接入；原创牌背 `arcana-starpath-mirror` 已接入运行时。
- 78 张分层牌义已 `approved`，运行时抽牌池已为完整牌组。
- AI 是否进入 Web 首发尚未决定。
- Web 原型的拖动、DOM 坐标测量和振动反馈在迁移 Taro/微信小程序时需要平台实现或适配，不能直接复制浏览器 API。
- 当前78张 WebP 牌面约16 MB；正式微信构建使用CloudBase按需读取，本地测试构建临时复制全部牌面并约18 MB，不得上传。
- 自动测试已确认78张路径规则；开发者工具已确认文件存在，但CloudBase公开读取权限尚未开通。
- 微信开发者工具项目使用正式 AppID `wxdee65d504fb068d2`；CloudBase环境为 `cloud1-d4gihrh6ob576fe1d`。
- 运行时高风险词扫描已清零并加入自动测试；内部历史文档仍保留研发术语，不作为公开页面内容。
- 正式应用当前仍将多个阶段集中在单个页面组件中；完成剩余牌阵后需要拆分页面与特性组件，避免继续扩大单文件复杂度。

## 验证记录

### 2026-07-20

- `prototype` 生产构建：通过。
- JSON Schema 与关联校验：22张牌、22个素材映射、5种牌阵通过。
- 牌义 Schema v2：1张完整样例和22张自动迁移草稿通过。
- 主题映射：4个启用分类全部具备显式牌义主题映射。
- 解读组合器：v2正位主题、v2逆位主题、牌位提示和v1回退测试通过。
- 标准卡牌索引：78个稳定ID、连续顺序、22张大牌和四组各14张小牌通过。
- RWS Manifest：78个素材槽位全部通过 Schema 和关联校验。
- 素材管线：78个待接入槽位通过路径、状态和空值校验；WebP空队列执行通过。
- Commons 来源审计：78个文件、78个唯一 SHA-1、统一作者/日期/许可字段通过。
- 问题引导校验：4个分类、每类4个选项通过。
- 抽牌领域测试：无重复抽取、固定随机源可复现，通过。
- 服务端渲染测试：通过。
- ESLint：通过。
- 浏览器人工交互评审：待完成。

### 2026-07-21

- 对话状态机：合法阶段转移、非法动作守卫和重置流程，共3项测试通过。
- 完整自动验证：数据、78张素材、构建、SSR、领域逻辑和对话流程共13项测试通过。
- ESLint：通过。
- 生产构建：通过。
- 浏览器人工验证：单牌与时间流完整流程通过。
- 抽牌动画：实际牌背起点、翻面、目标牌框落位和抽后回到对话通过。
- 圆环牌组：底部下沉构图、首尾缺口、鼠标/触摸旋转和点击抽牌通过。
- 分层牌义：22张大阿卡纳 v2 草稿全部通过 Schema、稳定 ID、六类主题和无占位文本校验。
- 详细解读：时间流默认折叠三张完整内容，逐张展开后正确显示主题、牌位、象征、明暗面、建议和反思问题。
- 深度润色：22张牌通过模板指纹、跨牌重复句、绝对预测与财务越界校验；完整测试18项通过。
- 待补：iOS Safari、Android Chrome 和微信小程序真机验证。

### 2026-07-23

- 共享核心迁移：78张牌义、78张已批准素材、5种牌阵和4类问题引导校验通过。
- Web 原型：TypeScript、生产构建及9个测试文件通过。
- 正式应用自动测试：抽牌、单牌组合、历史记录、今日一牌和时间流共10项通过。
- 正式应用质量门禁：TypeScript、ESLint、Prettier、H5 构建和微信小程序构建通过。
- 微信素材适配：三张样例和全部78张 CloudBase 映射测试通过；H5 构建包含78张本地牌面，微信构建包含0张正面牌、保留1张独立牌背，产物为1,146,316字节。
- 待补：iOS Safari、Android Chrome、微信开发者工具与微信真机主路径验证。

### 2026-07-24

- 正式应用类型检查：通过。
- 正式应用6个测试文件、15项测试：通过。
- 本地素材微信构建：通过；包含78张正面牌，产物约18 MB。
- CloudBase路径：File ID解析正确，文件存在；公开读取返回 `STORAGE_EXCEED_AUTHORITY`，待开通权限后复验。
- 微信开发者工具：正式AppID已配置；已处理旧 `vendors.js` 缓存和热重载依赖图问题。
- 文案合规：正式应用、共享内容与Web原型扫描无命中；新增门禁测试通过。
- Web原型：类型、数据、78张素材、生产构建、10个测试文件和ESLint全部通过。
- 正式应用：类型、ESLint、格式、15项测试及H5/微信生产构建全部通过。
- 待补：CloudBase付费权限、微信真机主路径和分享能力。

### 2026-07-27

- 正式应用类型检查、ESLint和Prettier：通过。
- 正式应用7个测试文件、20项测试：通过。
- H5生产构建：通过。
- CloudBase模式微信小程序生产构建：通过；正面牌继续按需读取，不复制进上传包。
- 本地素材微信小程序生产构建：通过；用于开发者工具复验今日一牌翻牌。
- 微信开发者工具新版本未展示“忽略未被引用的文件”开关；项目配置继续显式保留 `ignoreDevUnusedFiles: false`。
- 牌背图片显式启用小程序 WebP 解码，并在真机加载失败时回退为可见的符号牌背，避免选牌区域空白。
- 微信云存储牌面确认位于环境根目录 `{cardId}.webp`；翻牌页、结果页显式启用 WebP 解码，历史记录重新打开时先解析 CloudBase 临时地址。
- 修正微信构建命令语义：默认 `build:weapp` 改为 CloudBase 真机构建；78张本地素材仅由显式的 `build:weapp:local` 生成，避免真机误用本地 WebP。
- 真机验证确认网络牌面正常、本地 WebP 牌背不显示；将运行时牌背替换为720×1080基线 JPEG，源 PNG、授权和独立牌背映射保持不变。
- JPEG 牌背仍被真机忽略后，改为应用适配层静态导入；由 Taro/Vite 建立资源依赖并生成最终路径，不再依赖 JSON 动态路径或开发者工具的未引用文件设置。
- 真机截图确认牌背进入“✦”错误兜底；将小程序牌背引用由 `/assets/...` 修正为相对当前页面的 `../../assets/...`，H5 继续使用绝对静态路径。
- 真机继续无法读取相对包内路径后，牌背改为 Taro 静态导入的数据内联方案，彻底移除运行时路径依赖；同时将图片组件返回的 `errMsg` 显示在选牌页便于复验。
- 微信真机复验确认 Base64 牌背正常显示；问题归档为动态包内资源在真机调试链路中的依赖识别或路径解析不一致，保留 CloudBase 牌背作为后续减包方案。
- 浏览器与微信真机人工交互：待完成。

### 2026-07-29

- 对照 Web 抽牌工作区截图，将正式小程序选牌阶段改为上方配置化牌位槽、下方78张半圆牌组。
- 半圆牌组使用300°均匀圆弧布局；触摸横向拖动控制旋转，跨过8°刻度时触发轻触反馈，拖动结束后抑制误点击。
- 已选牌从圆弧移除并重新排布；牌位槽按牌阵配置顺序激活，抽出的正逆位牌面填入对应槽位。
- 洗牌阶段迁移左右分牌动效；翻牌阶段增加牌背到牌面的3D翻转、居中停留和向牌位方向收束的节奏。
- 新增半圆布局和拖动角度纯函数测试；正式应用8个测试文件共23项测试通过。
- 微信 CloudBase 构建通过，保持0张本地正面牌和 Base64 独立牌背策略。
- 微信真机反馈首版牌堆同时渲染78个按钮，拖动时需要逐帧更新全部节点，出现卡顿且约4°牌距过于拥挤。
- 牌组改为虚拟窗口：洗牌和抽牌语义仍覆盖完整78张，但界面只渲染中心附近13张；牌距扩大到11°、牌张缩小并高亮中央牌，拖动一个牌距后循环展示相邻牌。
- 抽牌动画按 Web 原型重新实现：点击时测量所选牌和当前牌位的真机坐标，卡牌从实际牌堆位置飞到屏幕中央，翻面展示正逆位牌面，再缩小飞入目标牌位。
- 半圆牌组测试扩展到可见窗口、循环索引、拖动换牌和入位动画几何；正式应用8个测试文件共24项测试通过。
- 真机复验反馈虚拟窗口无法明确到达牌库首尾，且 reveal 阶段卸载了抽牌工作区，动画背景和目标牌框不可见。
- 以 Web 原型为唯一效果基准：恢复78张完整300°扇形和 Web 尺寸比例，拖动只旋转一个牌组容器，避免逐张更新节点；旋转限制为 -150° 到 150°，两端分别将最后一张和第一张移到中央。
- reveal 阶段继续渲染原抽牌工作区，当前目标牌框保持空白并禁用交互；卡牌完成居中翻面后飞入该牌框，动画结束才填充牌面。
- 测试改为覆盖 Web 300°布局、完整78张、首尾聚焦、容器拖动比例和入位几何；正式应用8个测试文件共25项测试通过。
- 真机继续反馈点击后有停顿、抽牌位置立即闭合且拖动仍有卡顿；确认点击链路仍同步等待 CloudBase 临时地址，牌组节点虽然只旋转外层，但 React 仍在每次触摸时遍历78张牌。
- 洗牌阶段分批预热 CloudBase 临时地址并缓存并发请求；点击后立即振动、标记抽牌空位并启动动画，牌面地址在动画期间异步补齐，不再阻塞第一帧反馈。
- 已抽位置不再从牌组数组删除，而是保留半透明虚线空框；牌组其余位置和角度保持不变。
- 78张牌按钮改为 `useMemo` 复用，拖动仅约24ms提交一次容器角度并用32ms CSS线性插值补帧；触觉反馈恢复为 Web 的8°刻度，降低原生调用频率。
- 新增 CloudBase 50张分批预热、缓存复用和并发合并测试；正式应用9个测试文件共27项测试通过。
- 下一步打算：重新完成微信真机点击首帧、缺口可见性和连续拖动帧率复验；若低端机仍掉帧，再将手势旋转下沉到 WXS，随后接入正式应用圣三角牌阵。
- 按用户调整后的优先级先迁移正式分享：结果页复用共享核心生成统一分享预览，不复制牌义或牌阵数据。
- Web 端复用原型海报布局生成1080×1350 PNG，优先调用系统文件分享；不支持时自动退化为纯文本系统分享或剪贴板。
- 微信小程序页面启用好友转发与朋友圈分享，结果页增加原生好友分享按钮、右上角朋友圈提示和复制文案降级。
- 分享失败或取消不会影响查看、保存和重新开始；新增正式分享内容与五牌布局回归测试。
- 类型检查、ESLint、Prettier、10个测试文件共30项测试通过。
- H5生产构建和CloudBase微信小程序生产构建通过；微信构建保持0张本地正面牌，分享页面配置与原生按钮已进入产物。
- 下一步打算：在浏览器和微信真机验证分享图片、好友转发与朋友圈入口；随后恢复圣三角迁移。

### 2026-07-30

- 按新增需求将凯尔特十字纳入正式小程序：在共享 `spreads.json` 增加十个牌位、传统交叉结构视觉坐标与90度交叉牌配置。
- 正式应用牌阵入口改为读取应用配置，页面不再直接写死可选牌阵 ID；凯尔特十字复用完整78张洗牌、位置选牌、翻牌入位和历史记录链路。
- 增加凯尔特十字“问题脉络 / 行动参考”专属组合摘要；第十张定义为综合方向，不写成确定结局。
- 十牌抽牌区使用配置坐标紧凑展示，长牌位名由当前操作提示呈现；结果页继续使用摘要优先、逐张展开的通用结构。
- 分享卡布局由最多五张扩展为最多十张，凯尔特十字不会再丢失后五张牌。
- 新增十张无重复、牌位顺序、摘要边界、交叉布局、十牌历史与十牌分享回归测试。
- 共享数据 Schema 与关联校验通过，正式应用类型检查、ESLint、Prettier及11个测试文件共33项测试通过。
- H5与CloudBase微信生产构建通过；微信产物约1.4 MB，保持0张本地正面牌。
- 下一步打算：在微信开发者工具与真机验收十牌布局和连续十次翻牌性能，再继续圣三角迁移。

## Git 提交提醒

P1 首版原型已创建独立提交：

```text
feat(prototype): add interactive tarot reading flow
```

提交短哈希：`b30aa43`

本次资源适配提交建议为：

```text
feat(app): load weapp card assets from CloudBase
```

本次功能提交建议为：

```text
feat(app): bind selections to full tarot deck
```

本次功能提交建议为：

```text
feat(app): add cross-platform reading share
```

本次凯尔特十字提交建议为：

```text
feat(app): add celtic cross spread
```

原型早期提交 `b30aa43` 和 `75de867` 属于迁移前的内部原型仓库，仅作为历史记录保留。自根仓库初始化后，所有文档、原型、数据和正式工程均从 Arcana 根目录统一提交。

提交前必须再次确认构建和测试通过，不要把 `node_modules`、`dist`、本地环境文件或临时日志加入版本控制。

## 变更记录格式

后续每次推进按以下格式追加：

```text
日期：
完成：
验证：
决策：
风险：
下一步打算：
1.
2.
3.
需要确认：
```
