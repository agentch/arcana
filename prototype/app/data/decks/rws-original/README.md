# RWS Original 素材目录

当前目录只包含78张素材槽位和待核验元数据，不包含正式牌面文件。

来源与授权通过审核后：

- 未修改的扫描原文件放入 `source/`；
- 许可文本、来源页面存档和核验记录放入 `LICENSES/`；
- 运行 `npm run assets:build:web` 生成的文件放入 `web/`；
- `manifest.json` 记录每个文件的路径、格式、尺寸、大小和 SHA-256；
- 牌背只在 `card-backs.json` 中管理。

不要手工把来源不明的图片标记为 `approved`。
